<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="activity_transition" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@android:color/holo_blue_light</item>
        <item name="colorPrimaryDark">@android:color/holo_orange_light</item>
        <item name="colorAccent">@android:color/holo_green_light</item>
        <item name="android:windowAnimationStyle">@style/transition</item>
    </style>
    <style name="transition" parent="@android:style/Animation.Activity">

        <!-- when try to open new activity-->
        <item name="android:activityOpenEnterAnimation">@anim/slide_in_right</item>
        <!-- when try to pause previous activity -->
        <item name="android:activityOpenExitAnimation">@anim/slide_out_left</item>



        <!-- when press back to enter previous activity -->
        <item name="android:activityCloseEnterAnimation">@anim/slide_in_left</item>
        <!-- when press back to enter previous activity and destroy current activity -->
        <item name="android:activityCloseExitAnimation">@anim/slide_out_right</item>

    </style>

</resources>